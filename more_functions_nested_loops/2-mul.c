#include "main.h"

/**
 * mul - checks if uppercase letter
 * @a: - input of function
 * @b: - input of function
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
int t;
t = a * b;
return (t);
}
